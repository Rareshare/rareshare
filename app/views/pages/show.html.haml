%div.row.row-header{class: @page.slug}
  %div.span10.offset1.header-body
    %h1= @page.title

%div.row{class: @page.slug}
  %div.span10.offset1
    - if @page.pdf.present?
      %iframe{src: @page.pdf, width: "100%", height: "600px" }
    - else
      = @page.html_content
